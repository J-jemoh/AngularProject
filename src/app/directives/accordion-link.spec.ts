import { AccordionLink } from './accordion-link';

describe('AccordionLink', () => {
  it('should create an instance', () => {
    const directive = new AccordionLink();
    expect(directive).toBeTruthy();
  });
});
